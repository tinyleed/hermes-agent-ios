# Next

There is no active next implementation slice. Hermes Agent iOS is shelved and should not be advanced by default.

If this project is explicitly reopened:

1. Re-check the current Hermes Agent mobile/product direction.
2. Decide whether this repository still has a differentiated role.
3. Only then select a small, reversible, verified issue/PR slice.

Former next slice before shelving:

- Physical-device APNs sandbox push proof for issue #20 using a wake/attention-only `blocking_request_available` payload, with credentials, Team ID, provisioning state, and raw device tokens kept local/private.

That former slice is historical context, not an active instruction.
